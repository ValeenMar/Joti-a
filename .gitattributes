# =========================
# Reglas generales de texto
# =========================
* text=auto

# =========================
# Archivos de Unity como texto
# Esto ayuda a que Git compare mejor escenas, prefabs y metadatos.
# =========================
*.cs text diff=csharp
*.shader text
*.cginc text
*.hlsl text
*.compute text
*.uxml text
*.uss text
*.asmdef text
*.asmref text
*.json text
*.md text
*.meta text
*.unity text
*.prefab text
*.mat text
*.anim text
*.controller text
*.overrideController text
*.playable text
*.mask text
*.asset text
*.physicsMaterial2D text
*.physicMaterial text
*.terrainlayer text

# =========================
# Git LFS para binarios pesados
# Elegimos archivos fuente y multimedia grandes.
# No metemos .unity ni .prefab en LFS porque conviene versionarlos como texto.
# =========================
*.fbx filter=lfs diff=lfs merge=lfs -text
*.blend filter=lfs diff=lfs merge=lfs -text
*.max filter=lfs diff=lfs merge=lfs -text
*.mb filter=lfs diff=lfs merge=lfs -text
*.ma filter=lfs diff=lfs merge=lfs -text
*.psd filter=lfs diff=lfs merge=lfs -text
*.tga filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text
*.mp3 filter=lfs diff=lfs merge=lfs -text
*.ogg filter=lfs diff=lfs merge=lfs -text
*.flac filter=lfs diff=lfs merge=lfs -text
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.mov filter=lfs diff=lfs merge=lfs -text

